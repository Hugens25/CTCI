def three_in_one():
    """
    Describe how you could use a single array to implement three stacks.
    """

    return "Use the 2 ends of the array as the front of 2 stacks, and use the " + \
           "middle of the array as another stack. If/when any of the stacks" + \
           "breached the space of another, the array would be expanded to allow" + \
           "proper partitioning between each stack"